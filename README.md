# Game project

Steps to run the game. Execute in terminal.

``` sh
cd game
python3 main.py
```



# App project

Steps to run the game. Execute in terminal.

``` sh
git clone
cd app
source env/bin/activate
pip3 install -r requirements.txt
python3 main.py
```